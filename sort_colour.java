import java.util.*;
class Solution {
    public void sortColors(int[] nums) {
        Arrays.sort(nums);
        ArrayList<Integer>a=new ArrayList<>();
        for(int i=0;i<nums.length;i++){
            a.add(i);
        }
    }
}